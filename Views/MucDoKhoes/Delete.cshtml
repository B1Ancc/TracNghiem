@model TracNghiem.Models.MucDoKho

@{
    ViewBag.Controller = "Mức độ";
    ViewBag.Title = "Xóa mức độ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 class="text-danger">Xác nhận xóa mức độ?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            Mức độ
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TenMucDo)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <button type="submit" class="btn btn-danger" style="margin: 0px 5px;;">
        <i class="fas fa-trash"></i>
        <span>
            <strong>Xóa</strong>
        </span>
    </button>

    <a href="@Url.Action("Index", "MucDoKhoes")" class="btn btn-primary" style="margin: 0px 5px;;">
        <i class="fas fa-list"></i>
        <span>
            <strong>Danh sách</strong>
        </span>
    </a>
</div>
    }
</div>
