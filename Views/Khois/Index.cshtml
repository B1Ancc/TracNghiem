@model IEnumerable<TracNghiem.Models.Khois>

@{
    ViewBag.Controller = "Khối/lớp";
    ViewBag.Title = "Danh sách khối/lớp";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <p>
        <a href="@Url.Action("Create", "Khois")" class="btn btn-primary">
            <i class="fas fa-plus"></i>
            <span>
                <strong>Thêm mới khối/lớp</strong>
            </span>
        </a>
    </p>
</div>
<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="col-sm-9">
                    Khối/lớp
                </th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="col-sm-9">
                        @Html.DisplayFor(modelItem => item.TenKhoi)
                    </td>
                    <td class="col-sm-3">

                        <a href="@Url.Action("Details", "Khois", new {id = item.id })" class="btn btn-primary btn-sm" style="margin: 0px 5px;">
                            <i class="fas fa-info"></i>
                            <span>
                                Chi tiết
                            </span>
                        </a>

                        <a href="@Url.Action("Edit", "Khois", new {id = item.id })" class="btn btn-warning btn-sm" style="margin: 0px 5px;">
                            <i class="fas fa-edit"></i>
                            <span>
                                Sửa
                            </span>
                        </a>

                        <a href="@Url.Action("Delete", "Khois", new {id = item.id })" class="btn btn-danger btn-sm" style="margin: 0px 5px;">
                            <i class="fas fa-trash"></i>
                            <span>
                                Xóa
                            </span>
                        </a>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>
